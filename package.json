{
  "name": "fashionhub",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "FHUB_be",
    "FHUB_ui"
  ],
  "scripts": {
    "render:build": "npm ci && npm run frontend:build --workspace fhub_be",
    "build": "npm run frontend:build --workspace fhub_be",
    "start": "npm start --workspace fhub_be",
    "test": "npm test --workspace fhub_be && npm test --workspace fashion-hub"
  },
  "engines": {
    "node": "20.x"
  }
}
